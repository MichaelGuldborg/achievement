import accumulate from "./accumulate";
import sumByIndex from "./sumByIndex";
import round from "./round";
import pmod from "./pmod";

export const math = {
    accumulate,
    pmod,
    round,
    sumByIndex,
}
export default math;